/**
 * Seed script disabled.
 *
 * AEGIS is configured to run against real data only.
 * Populate the system through the application UI:
 *
 *   1. Create staff accounts via the Admin → Settings panel.
 *   2. Register patients through Patient Intake.
 *   3. Submit EMS pre-arrival reports via the Ambulances page.
 *
 * To bootstrap the first admin account, use the one-time setup
 * endpoint described in DEPLOY.md.
 */
console.log("Seed script is disabled — use the application UI to populate data.");
process.exit(0);
